
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY=
# Default Google Sheet ID (fallback for all FIRs)
GOOGLE_SHEET_ID=
# FIR-specific Google Sheet IDs (optional, falls back to GOOGLE_SHEET_ID)
GOOGLE_SHEET_ID_EDMM=
GOOGLE_SHEET_ID_EDGG=
GOOGLE_SHEET_ID_EDWW=

ADMIN_API_TOKEN=""

VATGER_CLIENT_ID=
VATGER_CLIENT_SECRET=

VATGER_TOKEN_URL=
VATGER_USER_INFO=
VATGER_CONNECT_URL=

VATGER_API=
VATGER_API_TOKEN=

# === Database Configuration ===
# Option 1: Use SQLite for local development (no MySQL required)
# Set USE_TEST_DB=true to use a local SQLite database for testing/development
# When enabled, DATABASE_URL should point to a SQLite file (e.g., file:./dev.db)
USE_TEST_DB=false

# Option 2: Use MySQL/MariaDB (production or local MySQL)
# When USE_TEST_DB=false, these DB_* variables are used
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=

# Database connection URL for Prisma (alternatively, use DB_* variables above)
# For SQLite: file:./dev.db
# For MySQL: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=

# Training system endpoints and token
TRAINING_API_TOKEN=
TRAINING_API_SOLOS_URL=
TRAINING_API_ENDORSEMENTS_URL=
TRAINING_API_FAMILIARIZATIONS_URL=
TRAINING_API_CPTS_URL=

# Cron schedule for automatic training data cache refresh (default: "0 3 * * *" = daily at 3:00 AM UTC)
# Format: minute hour day month weekday (see https://www.npmjs.com/package/node-cron for syntax)
TRAINING_CACHE_REFRESH_CRON=

# Cron schedule for event reminder notifications (default: "0 9 * * *" = daily at 9:00 AM UTC)
# Checks for events starting in 3 weeks where signup registration hasn't been opened yet
# Format: minute hour day month weekday (see https://www.npmjs.com/package/node-cron for syntax)
EVENT_REMINDER_CRON=


# VATSIM Sandbox
VATSIM_SANDBOX_CONNECT_URL=
VATSIM_SANDBOX_TOKEN_URL=
VATSIM_SANDBOX_USER_INFO=
VATSIM_SANDBOX_CLIENT_ID=
VATSIM_SANDBOX_CLIENT_SECRET=

DEV_MODE=