/**
 * Schalter für Funktionen, die fertig gebaut, aber noch nicht freigegeben sind.
 */

/**
 * Öffentliche Roster-Ansicht auf den Event-Seiten.
 *
 * Vorläufig aus: bis der Besetzungsplan-Editor produktiv freigegeben ist,
 * verweisen die Event-Seiten weiterhin auf den externen `rosterlink` wie
 * vor dem Umbau. Editor, API (`/roster/public`) und die Anzeige-Komponente
 * bleiben vollständig einsatzbereit – zum Aktivieren genügt `true`.
 */
export const PUBLIC_ROSTER_VIEW_ENABLED = false;
